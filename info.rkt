#lang info

(define collection 'multi)

(define deps (list "base"
                   "db-lib"))

(define build-deps '("racket-doc"
                     "rackunit-lib"
                     "scribble-lib"))
